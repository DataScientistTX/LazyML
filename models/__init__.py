# models/__init__.py

from .lazy_models import run_lazy_model

# You can add any initialization code for the models package here if needed

# Optionally, you can define __all__ to control what gets imported with "from models import *"
__all__ = ['run_lazy_model']